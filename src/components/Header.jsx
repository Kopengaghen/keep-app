import React from 'react';

const Header = () => {
    return (
        <header className="container">
            <h1 className="header">Keeper</h1>
        </header>
    );
};

export default Header;